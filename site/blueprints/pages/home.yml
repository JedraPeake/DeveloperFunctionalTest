title: Home
# pages: true

columns:
    - width: 2/3
      fields:
        body:
          type: textarea
          size: large
  - width: 1/3
      fields:
        text:
          type: text
          multiple: false
        cover:
          type: files
          layout: cards
          info: "{{ file.dimensions }}"
          template: cover
          min: 1
          max: 1
        toggle:
          label: Toggle
          type: toggle
          text: Include in menu?
        
        