title: Site
unlisted: true

tabs:
  pages:
    label: Pages
    preset: pages
    unlisted: true
    drafts:
      template: default
  analytics:
    fields:
      googleTag:
        label: Google Global Site Tag
        type: textarea
        size: small
      facebookTag:
        label: Facebook Pixel Site Tag
        type: textarea
        size: small
  header:
    fields:
      logoImage:
        label: Logo Image
        type: files
        layout: cards
        info: "{{ file.dimensions }}"
        multiple: false
        required: true
  footer:
    fields:
      footerText:
        label: Footer Text
        type: textarea
        size: small